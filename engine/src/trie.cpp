// Trie is a header-only template; this translation unit exists so the engine
// library has at least one non-header source (helps some build systems and
// keeps coverage tooling happy).
#include "sinhala_ime/trie.h"

namespace sinhala_ime {
// Intentionally empty.
}  // namespace sinhala_ime
